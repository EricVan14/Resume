// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    text: {
      primary: '#ffffff',
    },
  },
  typography: {
    allVariants: {
      color: '#ffffff',
    },
  },
});

export default theme;
